import { defineConfig } from '@api-bff/core';

export default defineConfig({
  prefix: '{{ prefix }}',
  openapi: {
    title: '{{ projectName }}',
  },
  caching: {
    strategy: 'file',
    ttl: 15_000,
  },
});
