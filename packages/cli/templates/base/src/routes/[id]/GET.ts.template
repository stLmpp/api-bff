import { apiConfig } from '@api-bff/core';
import { z } from 'zod';

export default apiConfig({
  host: 'jsonplaceholder.typicode.com',
  path: 'todos/:id',
  request: {
    validation: {
      params: z.object({
        id: z.string(),
      }),
    },
    mapping: {
      params: {
        id: 'id',
      },
    },
  },
});
