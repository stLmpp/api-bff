import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    globals: true,
    watch: false,
    coverage: {
      all: true,
      enabled: true,
      statements: 80,
      branches: 80,
      lines: 80,
      functions: 80,
    },
  },
});
